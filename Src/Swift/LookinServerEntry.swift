#if LOOKIN_SERVER_SWIFT_ENABLED

/// Public entry point for the LookinServer Swift subspec.
/// Reference `LookinServerEntry.self` in AppDelegate to ensure the
/// module is loaded and LKS_ConnectionManager is initialized.
public enum LookinServerEntry {}

#endif
